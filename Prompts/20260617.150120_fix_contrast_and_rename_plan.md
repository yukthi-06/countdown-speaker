# User Prompt

<USER_REQUEST>
the burget menu is not still visible because of color contrast issue. fix it 
Also app name is 'Speaking Counter'
</USER_REQUEST>

# Fix Contrast and Rename Plan

We will force the hamburger menu icon color to match the theme attribute `?attr/colorOnPrimary` by overriding the `drawerArrowStyle` parameter inside light and night themes, and update the application name from "SpeakTimer" to "Speaking Counter".

## Proposed Changes

### Configuration & Styles

#### [MODIFY] [themes.xml](file:///x:/Projects_X/0_Active/8_Android_APK/04_Early_Stage/Speaking_Timer_APK_GITHUB/app/src/main/res/values/themes.xml)
- Override `drawerArrowStyle` referencing a new `@style/DrawerArrowStyle` child style set to `?attr/colorOnPrimary`.

#### [MODIFY] [themes.xml (night)](file:///x:/Projects_X/0_Active/8_Android_APK/04_Early_Stage/Speaking_Timer_APK_GITHUB/app/src/main/res/values-night/themes.xml)
- Override `drawerArrowStyle` referencing a new `@style/DrawerArrowStyle` child style set to `?attr/colorOnPrimary`.

#### [MODIFY] [strings.xml](file:///x:/Projects_X/0_Active/8_Android_APK/04_Early_Stage/Speaking_Timer_APK_GITHUB/app/src/main/res/values/strings.xml)
- Rename "SpeakTimer" to "Speaking Counter".

## Verification Plan

### Manual Verification
- Check the navigation drawer menu toggle contrast and verify the application name displays as "Speaking Counter".
