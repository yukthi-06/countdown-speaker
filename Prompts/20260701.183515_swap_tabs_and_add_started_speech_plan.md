# Prompts
- of the two tabs, 'Count Up' should be first
- when a counter starts, and 'Speak aloud' is set, it should announce 'Started'
- create MD files please

# Goal
Swap the order of tabs so 'Count Up' is first, and add a Text-To-Speech announcement "Started" when a timer is started and 'Speak aloud' is selected.

# Proposed Changes
- `MainActivity.java`: Swap the tab titles in `TabLayoutMediator`.
- `TimerPagerAdapter.java`: Swap the fragment instantiation in `createFragment()`.
- `strings.xml`: Add a new string resource `<string name="tts_started_speech">Started</string>`.
- `CountdownFragment.java`: Add TTS announcement call in `startTimer()`.
- `CountupFragment.java`: Add TTS announcement call in `startTimer()`.
