# User Prompt

<USER_REQUEST>
the burget menu is not still visible because of color contrast issue. fix it 
Also app name is 'Speaking Counter'
</USER_REQUEST>

# Fix Contrast and Rename Tasks

- [x] Add `drawerArrowStyle` override to `Theme.SpeakTimer` in `values/themes.xml`
- [x] Add `drawerArrowStyle` override to `Theme.SpeakTimer` in `values-night/themes.xml`
- [x] Rename app strings in `values/strings.xml` to "Speaking Counter"
