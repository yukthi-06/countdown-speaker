# Prompts
- do the same announcement for 'Pause' and 'Resume' and 'Reset' actions

# Walkthrough
## What was done
1. **New Strings Added:** Added `tts_paused_speech`, `tts_resumed_speech`, and `tts_reset_speech` to `strings.xml`.
2. **Announcements Added:** Updated the `pauseTimer()`, `resumeTimer()`, and `resetTimer()` methods in both `CountdownFragment.java` and `CountupFragment.java`. If the "Speak aloud" checkbox is enabled, the timer will now use Text-To-Speech to say "Paused", "Resumed", or "Reset" respectively.

## What was verified
Verified that the speech announcements trigger correctly in all three lifecycle states of the timers without interfering with the existing countdown/countup logic.
