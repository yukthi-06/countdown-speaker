# User Prompt

<USER_REQUEST>
the burget menu is not visible because of color contrast issue. fix it 
</USER_REQUEST>

# Fix Hamburger Contrast Walkthrough

We resolved the color contrast bug on the drawer toggle and screen back arrows by routing icon tint overlays through the default dynamic themes.

## Changes Made

### Layouts
- Updated toolbars in `activity_main.xml`, `activity_settings.xml`, `activity_help.xml`, and `activity_about.xml` to color text and navigation actions using the themed value `?attr/colorOnPrimary` instead of the hardcoded color `@color/on_primary`.

## Verification
- Verified that toolbars load high-contrast styling in both dark and light modes.
