# User Prompt

<USER_REQUEST>
the burget menu is not visible because of color contrast issue. fix it 
</USER_REQUEST>

# Fix Hamburger Contrast Tasks

- [x] Change titleTextColor and navigationIconTint to `?attr/colorOnPrimary` in `activity_main.xml`
- [x] Change titleTextColor and navigationIconTint to `?attr/colorOnPrimary` in `activity_settings.xml`
- [x] Change titleTextColor and navigationIconTint to `?attr/colorOnPrimary` in `activity_help.xml`
- [x] Change titleTextColor and navigationIconTint to `?attr/colorOnPrimary` in `activity_about.xml`
