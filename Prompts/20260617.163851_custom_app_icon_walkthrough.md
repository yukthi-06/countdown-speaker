# User Prompt

<USER_REQUEST>
use 'vecteezy_icon-symbol-education-atl_11684633.png' as app icon.  Also in About page
</USER_REQUEST>

# Custom App Icon Walkthrough

We integrated references to the custom image icon in the manifest and about page layouts.

## Changes Made

### Configuration
- Changed `android:icon` and `android:roundIcon` in `AndroidManifest.xml` to `@drawable/vecteezy_icon_symbol_education_atl_11684633`.

### Layouts
- Updated `activity_about.xml` ImageView src to reference `@drawable/vecteezy_icon_symbol_education_atl_11684633`.

## Verification
- User will run the copy command in CMD to place the root file inside drawables.
