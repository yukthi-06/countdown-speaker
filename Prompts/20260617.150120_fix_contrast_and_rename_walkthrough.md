# User Prompt

<USER_REQUEST>
the burget menu is not still visible because of color contrast issue. fix it 
Also app name is 'Speaking Counter'
</USER_REQUEST>

# Fix Contrast and Rename Walkthrough

We resolved the hamburger toggle visibility by explicitly mapping drawerArrowToggle coloring parameters to themed values and renamed strings referencing the app.

## Changes Made

### Themes
- Overrode the `drawerArrowStyle` parameter under both light and dark profiles to bind the hamburger draw color to `?attr/colorOnPrimary`.

### Strings
- Updated `strings.xml` to rename `app_name`, `nav_header_title`, and associated description tags to "Speaking Counter".

## Verification
- Checked that hamburger bars render in proper contrasting colors.
