# Prompts
- do the same announcement for 'Pause' and 'Resume' and 'Reset' actions

# Goal
Add Text-To-Speech announcements for "Paused", "Resumed", and "Reset" actions when a timer is manipulated and 'Speak aloud' is checked.

# Proposed Changes
- `strings.xml`: Add new string resources `<string name="tts_paused_speech">Paused</string>`, `<string name="tts_resumed_speech">Resumed</string>`, `<string name="tts_reset_speech">Reset</string>`.
- `CountdownFragment.java`: Add TTS announcement calls in `pauseTimer()`, `resumeTimer()`, and `resetTimer()`.
- `CountupFragment.java`: Add TTS announcement calls in `pauseTimer()`, `resumeTimer()`, and `resetTimer()`.
