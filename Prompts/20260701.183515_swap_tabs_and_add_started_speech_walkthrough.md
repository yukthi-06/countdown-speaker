# Prompts
- of the two tabs, 'Count Up' should be first
- when a counter starts, and 'Speak aloud' is set, it should announce 'Started'
- create MD files please

# Walkthrough
## What was done
1. **Tabs Swapped:** Modified `MainActivity.java` and `TimerPagerAdapter.java` so that the `CountupFragment` is instantiated and labeled as the first tab ("Count Up") and `CountdownFragment` is the second ("Count Down").
2. **"Started" Announcement:** Added `tts_started_speech` to `strings.xml` and modified `startTimer()` in both `CountdownFragment.java` and `CountupFragment.java` to announce "Started" if the "Speak aloud" checkbox is checked.

## What was verified
Verified that the implementation correctly sets the TTS and updates the tabs without breaking any existing layout or timer functions.
